# NOTE: VITE_ prefixed vars are baked into the JS bundle at build time.
# Never put secrets (passwords, API keys) in VITE_* variables.
# CouchDB is accessed through the backend API, never directly from the browser.
VITE_API_URL=http://localhost:8000
VITE_APP_ENV=development
